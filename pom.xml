<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.xiaoxusop</groupId>
    <artifactId>mcp-sentinel</artifactId>
    <version>0.5.5</version>
    <packaging>jar</packaging>

    <name>mcp-sentinel</name>
    <description>MCP 工具面安全扫描器：指纹化并锁定 MCP 服务器的工具定义，检测 schema 投毒 / 工具影子 / rug pull，可作 CI 门禁</description>
    <url>https://github.com/XIAOXUsop/mcp-sentinel</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mcp.sdk.version>2.0.1</mcp.sdk.version>
        <!--
          2.21.5 而不是 2.19.0：Dependabot 在这个仓库上报了 5 条 jackson-databind 告警，
          全部命中 2.19.0。逐条看下来要求的修复版本最高是 2.21.5：

            CVE-2026-54512 (HIGH)   多态类型校验器绕过          修复 2.21.4
            CVE-2026-54513 (HIGH)   数组子类型白名单绕过         修复 2.21.4
            CVE-2026-54514 (MEDIUM) InetSocketAddress 触发即时 DNS 解析  修复 2.21.4
            CVE-2026-59888 (MEDIUM) Record 上的 @JsonIgnore 被绕过      修复 2.21.4
            CVE-2026-54515 (MEDIUM) 逐属性大小写不敏感的反序列化绕过     修复 2.21.5

          取 2.21.5 = 修复线（最新的 2.21.6 也存在，但那不是任何一条的要求；
          与 amlagent 那次给 log4j 取 2.25.4 而非 2.25.5 是同一种取舍）。
        -->
        <jackson.version>2.21.5</jackson.version>
        <junit.version>5.13.4</junit.version>
        <!--
          可复现构建：固定产物里每个条目的时间戳。

          这个仓库把 jar 发到 GitHub Release。不固定的话，jar 里条目的时间就是
          "构建那一刻"，同一份源码在任何两个时刻构建出的字节都不同——
          「你下载到的这个 jar 是不是从这份源码构建出来的」就**无法用哈希回答**。

          实测（2026-09-19，加这个属性之前）：`./mvnw clean package -DskipTests`
          连做两次，产物大小相同（6098536 字节）但 sha256 不同：
             75f7f1c8df94a55dd49e3ee307439ed8…
             823472f009fd717d38e0158e2a206a8f…
          加完之后两次完全相同（见 README 的「可复现构建」一节）。

          配套的还有 `.mvn/wrapper/maven-wrapper.properties` 里钉住的 Maven 版本：
          时间戳只是"同一台机器两次构建相同"的保证，**跨机器**还需要构建工具一致
          （否则 `META-INF/MANIFEST.MF` 里的 `Created-By` 会不一样）。

          代价：jar 里的时间戳不再反映真实构建时间。对可验证性是划算的。
        -->
        <project.build.outputTimestamp>2026-09-19T00:00:00Z</project.build.outputTimestamp>
    </properties>

    <dependencies>
        <!--
          连接 MCP 服务器（仅适配层使用；指纹与规则是纯逻辑，不依赖它）。

          刻意用 mcp-core + mcp-json-jackson2，而不是 mcp 聚合包：
          聚合包会拉入 mcp-json-jackson3，而 Jackson 2 与 Jackson 3 的
          com.fasterxml.jackson.annotation 包同名不同代，无法共存于同一 classpath
          （实测报 NoClassDefFoundError: JsonSerializeAs）。
        -->
        <dependency>
            <groupId>io.modelcontextprotocol.sdk</groupId>
            <artifactId>mcp-core</artifactId>
            <version>${mcp.sdk.version}</version>
        </dependency>
        <dependency>
            <groupId>io.modelcontextprotocol.sdk</groupId>
            <artifactId>mcp-json-jackson2</artifactId>
            <version>${mcp.sdk.version}</version>
        </dependency>
        <!--
          MCP SDK 依赖 slf4j-api 但不带实现，于是每次调用都会打三行
          "No SLF4J providers were found" 警告。本工具不需要日志，挂个空实现把它消掉。
        -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-nop</artifactId>
            <version>2.0.16</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!--
          传递依赖里的 jackson-dataformat-yaml 也要拉到**同一条修复线**。

          它来自 `io.modelcontextprotocol.sdk:mcp-json-jackson2` → `jackson-dataformat-yaml:2.18.4`
          ——也就是打出来的 shaded jar 里，databind/core 是 2.21.5 而 yaml 那条还是 2.18.4。
          翻遍 README 与 pom 都看不到这件事：两处写的都是「内嵌 jackson-databind 2.21.5」，
          而按那句话去核对的人会看到 databind 对了就停下。

          实测（2026-09-22，解 shaded jar 看各构件的 pom.properties）：

              jackson-databind          2.21.5
              jackson-core              2.21.5
              jackson-annotations       2.21     ← 这个构件本来就是两段式版本号，2.21 即最新
              jackson-dataformat-yaml   2.18.4   ← 与上面几条差 3 个 minor

          取 2.21.5 而不是最新的 2.22.2：这个仓库一以贯之的原则是**取修复线，不是最新版**
          （见上面那段 5 条 CVE 的注释）。这里只是让同一次打包里的 jackson 构件落在同一条线上。
        -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>mcp-sentinel</finalName>

        <!--
          打开资源过滤：把 `${project.version}` 写进产物里那份版本说明。

          ── 为什么非做不可 ────────────────────────────────────────────
          版本号原先在**两个地方手写死**，而且都落后于 pom：

            SarifWriter.java   driver.version = "0.2.0"   （pom 是 0.5.3）
            McpConnector.java  clientInfo     = "0.5.1"

          也就是说，一个 0.5.3 的产物对外自称是 0.2.0 / 0.5.1——SARIF 里那份
          会进 code scanning 的告警记录，MCP 那份会发给握手对端。
          两次升版（0.5.2 → 0.5.3）都漏了，因为它们既不进门禁也不进任何断言。

          手写死的东西**必然**会漂。所以这里不是"记得同步"，而是让版本号只有一个来源：
          pom 的 `<version>` 过滤进 `mcp-sentinel-version.properties`，
          代码只从那里读（见 `Version.java`）。
        -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <plugins>
            <!--
              把 LICENSE 打进 jar 的 META-INF。
              这个 jar 里有好几份依赖的许可证（shade 带过来的），但**没有它自己的**——
              一个可自由再分发的产物必须自带它的许可条件。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>copy-license-into-jar</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/META-INF</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}</directory>
                                    <includes>
                                        <include>LICENSE</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <!--
                                  追加而不是覆盖 META-INF/LICENSE：本项目自己的 MIT 许可证与
                                  依赖带来的许可证会在同一个路径上撞车，而 shade 默认保留先遇到的
                                  那一份——**静默丢掉另一个**。实测确认过：加了自己的 LICENSE 之后，
                                  原本那份 11358 字节的 Apache 许可证就从 jar 里消失了。
                                -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                                    <resource>META-INF/LICENSE</resource>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>io.github.xiaoxusop.mcpsentinel.cli.Main</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
